const express = require("express");

const casting_pelicula = require("./casting_pelicula.router");
const heroes = require("./heroes.router");
const image = require("./image.router");
const movie = require("./movies.router");
const users = require("./users.router");
const router = require("./casting_pelicula.router");


module.exports = router;
